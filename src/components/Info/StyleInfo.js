import {makeStyles} from "@material-ui/core";

const StyleInfo = makeStyles((theme) => ({
    childItem: {
        backgroundColor: '#ccc',
        padding: '10px 15px',
        borderRadius: '5px'
    },
}));

export default StyleInfo;