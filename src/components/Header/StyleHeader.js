import {makeStyles} from "@material-ui/core";

const StyleHeader = makeStyles((theme) => ({
    menuItem: {
        textDecoration: "none",
        color: '#000',
        display: "flex",
        alignItems: 'center',
        width: '100%'
    },
}));

export default StyleHeader